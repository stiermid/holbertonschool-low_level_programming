#ifndef VARIADIC_FUNCSIONS_H
#define VARIADIC_FUNCSIONS_H

#include <stdarg.h>
#include <stdio.h>
#include <stddef.h>

int _putchar(char c);
int sum_them_all(const unsigned int n, ...);
void print_numbers(const char *separator, const unsigned int, ...);

#endif

