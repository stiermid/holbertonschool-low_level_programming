#include "main.h"

/**
 * _abs - returns absolute value of an integer
 * @n: integer to process
 *
 * Return: absolute value of the given int
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}

